{
  "name": "@debrute/photoshop-uxp-plugin",
  "version": "0.0.4",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @debrute/app-protocol build && tsc --noEmit && vite build",
    "check": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@debrute/app-protocol": "workspace:*",
    "fflate": "0.8.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.10"
  }
}
